# POSIX CLI

 * 맨 끝에 물결 표시(~)는 현재 홈 디렉터리에 있다는 의미

## 현재 디렉터리 경로 확인
```bash
pwd
```

## 디렉터리 목록 확인
```bash
ls
```

```bash
ls -la
```

```bash
ls -l   # 상세 목록
ls -a   # 숨김 파일 포함 목록
ls -la  # 상세 + 숨김 파일 포함 목록
ls -al  # 상세 + 숨김 파일 포함 목록
```
[!TIP] 옵션은 순서와 상관없이 조합해서 사용할 수 있습니다.